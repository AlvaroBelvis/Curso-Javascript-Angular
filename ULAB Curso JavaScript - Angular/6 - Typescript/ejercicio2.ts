let myName: string = "Miguel";                  //Definimos una variable de tipo string

let printName = (name: string) => {             //Función que imprime por pantalla el string
    console.log(name);
}

printName(myName);                              //Imprimimos el nombre