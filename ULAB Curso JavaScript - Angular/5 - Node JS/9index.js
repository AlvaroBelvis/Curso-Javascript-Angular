const lodash = require('lodash');                       //Cargamos el modulo lodash del archivo package.json

let nombres = ["Alex", "Arturo", "Pablo", "Nacho"];     //Creamos un array con nombres

console.log(lodash.join(nombres));                      //Utilizamos la función join de lodash para mostrar el array con comas